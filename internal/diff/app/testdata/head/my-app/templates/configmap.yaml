apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config
  labels:
    app: {{ .Chart.Name }}
data:
  LOG_LEVEL: {{ .Values.logLevel }}
  CACHE_ENABLED: "{{ if .Values.env }}true{{ else }}false{{ end }}"
