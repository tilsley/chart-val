# Optional: Override Makefile defaults
#
# All configuration has defaults in the Makefile:
#   GITHUB_APP_ID=2814878
#   GITHUB_INSTALLATION_ID=108584464
#   WEBHOOK_SECRET=test
#   GITHUB_PRIVATE_KEY (loaded from chart-sentry.pem)
#
# Only create a .env file if you need to override these values.

# Example overrides:
# GITHUB_APP_ID=your-app-id
# WEBHOOK_SECRET=your-secret
# PORT=8080
# LOG_LEVEL=debug
